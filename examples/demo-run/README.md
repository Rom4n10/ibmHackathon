# Demo run

Put here the `scoreboard.json` of the real run shown in the video
(copied from `<demo-repo>/.antibody/runs/<run_id>/scoreboard.json`).

Open it with `ui/scoreboard.html?src=../examples/demo-run/scoreboard.json`
while serving the repo root with `python -m http.server 8000`.
