"""Antibody: an immune system for your repository, built on IBM Bob.

Bob thinks, the CLI executes: Bob (in the Antibody custom mode) diagnoses the
root cause, hunts for twins, writes tests, fixes and rules. This package runs
the deterministic parts (tests, Semgrep, vaccine rounds, scoring) and records
the evidence, so every claim in a report comes from a real execution.
"""

__version__ = "0.1.0"
